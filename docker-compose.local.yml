services:
  star-admin:
    container_name: star-admin-local
    image: star-admin:latest
    ports:
      - "3000:3000"  # Access at http://localhost:3000
    env_file:
      - .env
    restart: unless-stopped
